export declare class SharedUtils {
    static isUuid(str: string): boolean;
    private static getEncodedImageName;
    static getMediaWikiImagePath(imageName: string, includeFileName?: boolean): string;
}
